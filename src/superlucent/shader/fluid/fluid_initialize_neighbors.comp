#version 450

layout (local_size_x = 256) in;

#include "simulation_params.glsl"
#include "neighbors.glsl"

void main()
{
  uint index = gl_GlobalInvocationID.x;
  if (index >= params.max_num_neighbors)
    return;

  if (index == 0)
    num_neighbors = 0;

  neighbors_heads[index] = -1;
}
