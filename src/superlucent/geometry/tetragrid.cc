#include <superlucent/geometry/tetragrid.h>

#include <superlucent/geometry/mesh.h>

namespace supl
{
namespace geom
{
Tetragrid::Tetragrid() = default;

Tetragrid::~Tetragrid() = default;

void Tetragrid::FromMesh(const Mesh& mesh, const MeshOptions& options)
{
  // TODO
}
}
}
